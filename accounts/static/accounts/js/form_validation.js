document.addEventListener('DOMContentLoaded', function() {
    // This file previously contained validation for middle_name
    // Since we now use full_name instead, this validation is handled in validation_rules.js
    const form = document.querySelector('form');
    
    // Keeping empty event listener setup for potential future form validations
});